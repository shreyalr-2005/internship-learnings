1.What is the use of Venv?
    “venv is used to create an isolated environment for each project so that project dependencies (libraries) do not conflict with each other.”

2.How to see what are the libraries installed?
     pip list

3.How to tell if the libraries are installed globally or locally
     We can check the terminal prompt. If the virtual environment is active (shown as(venv)), libraries are local. Otherwise, they are global.

4.How to find difference between any two files or how to compare any two files in vscode
      In VS Code, we can compare two files by selecting one file, then right-clicking the other file and choosing ‘Compare with Selected

5.Name any 3 python frameworks that you know
      Django → full framework (big projects)
    Flask → lightweight, simple
    FastAPI → modern, fast APIs
6.What is python-jose library used for?
      python-jose is used for handling JWT (JSON Web Tokens), including creating, decoding, and verifying tokens for authentication.
7.What is jwt ?
    JWT (JSON Web Token) is a token used for securely transmitting information between client and server, mainly for authentication.

8.What is json?
    JSON (JavaScript Object Notation) is a lightweight data format used to store and exchange data between client and server.

9.What is root directory while deploying in Render ?
     Root directory in Render is the main folder of the project where the application code exists and from which deployment starts.

10.What is docker?
    Docker is used to package an application with its environment so it can run anywhere.

11.Why we use docker?
     Docker is used to ensure the application runs in the same environment everywhere, avoiding environment-related issues.

12.https://github.com/ManasaHM2005/To-do-backend.git deploy this using docker
     

